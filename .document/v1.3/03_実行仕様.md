# v1.3 実行仕様

## BAT-LOCAL-EXEC-001

- 実行方式: 手動
- 実行環境: Windows上のGit Bash
- 実行ユーザ: ローカルPCの開発ユーザ
- 事前操作:
  1. `cd /c/Users/yuki_/OneDrive/Documents/github/product_blog_ryutai`
  2. `git checkout develop`
  3. `git pull --ff-only origin develop`
- 実行コマンド: `./deploy_local.sh`
- 定期実行: なし
- タイムゾーン: 該当なし（手動）
- 多重起動: 想定しない。並列実行しない。
- 停止方法: Ctrl+C。コピー途中の場合は再実行で復旧する。
- 終了コード: 0=成功、0以外=失敗
- タイムアウト: スクリプト側では固定しない。HTTP起動確認は短時間のリトライを行う。

`deploy_local.sh` 自身は `git pull` を実行しない。pullは明示的な事前手順とし、意図しないリポジトリ更新を避ける。
