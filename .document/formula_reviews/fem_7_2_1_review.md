# 数式レビュー: fem/fem_7_2_1.html

更新日: 2026-09-17

## 対象

- ページ: `fem/fem_7_2_1.html`
- タイトル: 質量収支式の離散化（4面体1次要素）
- 数式画像: `img/fem_d_mass_tet.files/image001.png` ～ `image020.png`
- 対象数: 20式
- 状態: **元画像完全一致の再監査対象 / 対応付け20/20 / 13/20式を元画像目視監査済み**

## 判定基準

- 数学的に同値でも元画像と表記が違えばNG。
- `x/y/z` と `X/Y/Z`、`τ+Δτ` と `Δτ+τ` を区別する。
- 元画像の途中計算、先頭 `=`、括弧、行列展開を省略・補完しない。
- 誤記候補も原文式では元画像どおり残し、理論上の補足は式外へ分離する。
- Pass 1は元画像とLaTeXソース、Pass 2は元画像と実ブラウザMathJax描画を別判定する。

## 対応付け

監査候補 `fem_7_2_1_mathjax_audit.html` の20式は、`image001.png`～`image020.png` と同番号で1対1対応している。

**対応付け: 20/20 OK。**

## Pass 1 厳密監査

| No. | 元画像 | 目視 | 現候補 | 主な差分 |
|---:|---|---|---|---|
| 1 | `image001` | 済 | NG | 元画像は対流項 `x/y/z`、発散項 `X/Y/Z`。候補は発散項も小文字化 |
| 2 | `image002` | 済 | NG | 元画像は `∫_V[0,0,0,0]^T dV` の途中段階を明示。候補は省略 |
| 3 | `image003` | 済 | NG | 元画像は列ベクトル×節点値ベクトルと `[N]^T{V_x}` 等を表示。候補は途中式と転置を変更 |
| 4 | `image004` | 済 | NG | 元画像は圧力対流項 `x/y/z`、発散項 `X/Y/Z`。候補は圧力対流項も大文字化 |
| 5 | `image005` | 済 | NG | 元画像は前式から続く先頭 `=`。候補は左辺を補完 |
| 6 | `image006` | 済 | NG | 元画像の時刻添字は `Δτ+τ`。候補は `τ+Δτ` |
| 7 | `image007` | 済 | NG | 同上。自由度を積分外へ出した後も元画像は `Δτ+τ` |
| 8 | `image008` | 未 | HOLD | 元画像直接確認待ち |
| 9 | `image009` | 未 | HOLD | 元画像直接確認待ち |
| 10 | `image010` | 未 | HOLD | 元画像直接確認待ち |
| 11 | `image011` | 未 | HOLD | 元画像直接確認待ち |
| 12 | `image012` | 未 | HOLD | 元画像直接確認待ち |
| 13 | `image013` | 済 | NG | 元画像は階乗を含む体積積分公式の途中計算を明示。候補は最終値だけに要約 |
| 14 | `image014` | 未 | HOLD | 元画像直接確認待ち |
| 15 | `image015` | 未 | HOLD | 元画像直接確認待ち |
| 16 | `image016` | 済 | NG | 元画像は先頭 `=` と時刻添字 `Δτ+τ`。候補は `τ+Δτ` |
| 17 | `image017` | 済 | NG | 元画像は `=[0,0,0,0]^T`。候補は先頭 `=` を省略 |
| 18 | `image018` | 済 | NG | 元画像は `Δτ+τ`。候補は `τ+Δτ` |
| 19 | `image019` | 済 | NG | 元画像は `Δτ+τ`。候補は `τ+Δτ` |
| 20 | `image020` | 済 | NG | 元画像は `Δτ+τ`。候補は `τ+Δτ` |

### image013 元画像の重要な表記

元画像には最終値だけでなく、次の途中計算が明示されている。

- `i ≠ j`: `1!1!/(1+1+3)! × 6V = 6/5! V = 1/20 V`
- `i = j`: `2!/(1+1+3)! × 6V = 12/5! V = 1/10 V`
- `∫_V L_i dV`: `1!/(1+3)! × 6V = 6/4! V = 1/4 V`

原画像上の表記をそのまま転記し、理論的な整理は原文式とは分離する。

## 現在の集計

- 対応付け: **20/20**
- 元画像目視監査: **13/20**
- 現監査候補のPass 1合格: **0/20**
- 要修正: **13/20**
- 未監査: **7/20** (`image008～012`, `image014～015`)
- Pass 2: **0/20**

先頭7式と後半6式の監査結果から、このページも既存MathJax候補の微修正ではなく、**20式すべてを元画像忠実転記で再作成する対象**とする。

## 補足事項

### 対流速度の扱い

ページ前半で速度を形状関数内挿する一方、後続の対流項では `V_x,V_y,V_z` が積分外の係数として扱われる。既知速度を用いる線形化等が考えられるが、これは説明上の補足として扱い、原文式を変更しない。

### 体積 `V` の符号

積分公式は正の幾何学体積を想定するが、行列式定義では節点順序により符号付き体積となり得る。これも補足事項として原文式とは分離する。

## 再監査チェック

- [x] `image001`～`image020` を全て列挙。
- [x] 全MathJax式と元画像を1対1対応。
- [ ] 現候補20式のPass 1監査完了（13/20）。
- [ ] 元画像忠実版20式の再転記完了。
- [ ] 再転記版Pass 1合格20/20。
- [ ] Pass 2完了20/20。
- [ ] 行列全成分、`c_{ix},c_{iy},c_{iz}`、`1/Ma^2` を元画像と照合。
- [x] 誤記・理論補足は原文式から分離する方針を確定。
- [ ] PC/スマホ表示確認済み。

## 現在の判定

**未完了 / 対応付け20/20、現候補Pass 1目視13/20、合格0/20、要修正13/20、未監査7/20、Pass 2=0/20。**
