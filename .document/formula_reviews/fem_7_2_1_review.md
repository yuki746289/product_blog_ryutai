# 数式レビュー: fem/fem_7_2_1.html

更新日: 2026-09-19

## 対象

- ページ: `fem/fem_7_2_1.html`
- タイトル: 質量収支式の離散化（4面体1次要素）
- 正本: `img/fem_d_mass_tet.files/image001.png` ～ `image020.png`
- 元画像数: **20**
- MathJaxブロック数: **20**
- 新厳密 Pass 1: **20/20**
- 新厳密 Pass 2: **20/20**
- HOLD: **0**
- 通常ページ反映: **完了**
- Browser QA: **PASS**

## 2026-09-19 新厳密監査方式

1. 元HP画像を最終正本とする。
2. 既存LaTeXと旧再転記記録は候補として利用し、最終判定は元画像で行う。
3. 元画像 ↔ MathJax完成描画を20式すべてペア画像化して直接比較する。
4. 数学的同値化はしない。式範囲・項順・符号・添字・大文字小文字・改行・句読点まで正本に合わせる。
5. `x/y/z` と `X/Y/Z`、`Δτ+τ` と `τ+Δτ`、`i/j` は重点確認する。
6. 理論上の誤記候補も原文式では修正しない。

## 重点確認結果

- `image001`: 圧力対流項は **x/y/z（小文字）**、速度発散項は **X/Y/Z（大文字）**。
- `image004`: 同様に圧力対流項は小文字、発散項は大文字。
- 時刻添字は対象式すべて **`Δτ+τ`**。ページ内の `τ+Δτ` は **0件**。
- `image009`: 原画像どおり対流3項末尾を `{V_x}`, `{V_y}`, `{V_z}` として保持。
- `image013`: `(i\ne j)` / `(i=j)` を原画像どおり保持。
- `image014/015`: 一般化せず、元画像の各4×4行列を明示した状態を保持。

## 今回修正した差異

- `image002`: 3段階の等式を原画像どおり3行へ復元。
- `image003`: 元画像にない行末カンマ・ピリオドを除去。
- `image004`: 左辺と残差代入式を原画像どおり別行化。
- `image013`: `i\ne j` / `i=j` の括弧を復元し、原画像にないカンマを除去。
- `image016`: 蓄積項、対流項、`1/Ma^2` 項を原画像どおり3行化。
- `image019`: 蓄積項、対流項、残りの項＋右辺を原画像どおり3行化。
- `image020`: 係数行と `1/Ma^2` 項＋右辺を原画像どおり2行化。

## Browser / MathJax Pass 2

再利用可能な `Strict Formula Page QA` ワークフローで確認。

| Viewport | Math blocks | Rendered | MathJax error | Unrendered | Uncontained overflow | Page overflow |
|---|---:|---:|---:|---:|---:|---|
| desktop 1440x1000 | 20 | 20 | 0 | 0 | 0 | 0 |
| mobile 390x844 | 20 | 20 | 0 | 0 | 0 | 0 |

- mobileでは長い式9ブロックが `.math-block` 内スクロールになる。
- ページ全体の横overflowはない。
- 旧数式画像要素は通常ページ上 **0**。
- 元画像 ↔ MathJax比較ペア画像を `image001～image020` について生成して確認済み。

## 判定

**fem_7_2_1: 新厳密再監査 完了。**

旧レビュー判定より、2026-09-19の本結果を正とする。
