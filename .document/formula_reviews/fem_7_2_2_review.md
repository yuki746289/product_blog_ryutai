# 数式レビュー: fem/fem_7_2_2.html

更新日: 2026-09-19

## 対象

- ページ: `fem/fem_7_2_2.html`
- タイトル: 運動量収支式の離散化（4面体1次要素）
- 正本: `img/fem_d_momentum_tet.files/image001.png` ～ `image049.png`
- 元画像数: **49**
- MathJaxブロック数: **51**（`image031` / `image037` の意図した再掲を含む）
- 新厳密 Pass 1: **49/49**
- 新厳密 Pass 2: **49/49**
- HOLD: **0**
- 通常ページ反映: **完了**
- Browser QA: **PASS**

## 2026-09-19 新厳密監査方式

1. 元HP画像を最終正本とする。
2. Word数式は機械可読な転記元・比較候補として利用する。
3. 既存LaTeXは捨てず、Wordとの差分箇所だけ重点確認する。
4. 元画像 ↔ MathJax完成描画を49式すべてペア画像化して直接比較する。
5. 数学的同値化はしない。式範囲・項順・符号・添字・改行を正本に合わせる。
6. `t / τ`, `i / j`, `x / y / z`, 大小文字、添字、上付き、`∂ / d`, `+ / -` は全体類似度とは別に確認する。
7. 読み切れない箇所は推測せず HOLD とする。

## 今回の再監査で確定した重要事項

- `image005`: 圧力節点添字は原画像どおり **`P_{z,1}～P_{z,4}`**。理論的推測で変更しない。
- `image006`: 左辺 `∫[N]^T φ_i dV` と右辺の残差積分までが同一画像。時間記号は **`τ`**。
- `image007`: `image006` の続きとして、個別積分への展開を記載する。
- `image011` / `image012`: 表面張力項は **`-2K^*/We`**。二重負号ではない。
- `image016`: 元画像内の注記 **「蓄積量」** を通常HTML側にも復元。
- `image030`: `(i\ne j)` / `(i=j)` を確認。
- `image043～049`: `τ`、`i` 添字、圧力項位置、交差項を直接確認。
- 最終3成分式の交差項:
  - x: `[S_{yx}]`, `[S_{zx}]`
  - y: `[S_{xy}]`, `[S_{zy}]`
  - z: `[S_{xz}]`, `[S_{yz}]`

## 改行の再監査

元画像とMathJax完成描画を比較し、以下の改行差を修正した。

- `image005`: `V_x` の恒等式を原画像どおり1行化。
- `image011～015`: 表面張力項と重力項を別行化。
- `image043`: 表面張力項と重力項を別行化。
- `image044`: 蓄積項と対流項、表面張力項と重力項をそれぞれ別行化。
- `image047～049`: 圧力項を粘性項と同じ行へ戻す。

## Browser / MathJax Pass 2

GitHub Actions `FEM 7-2-2 MathJax Browser QA` で確認。

| Viewport | Math blocks | Rendered | MathJax error | Unrendered | Uncontained overflow | Page overflow |
|---|---:|---:|---:|---:|---:|---|
| desktop 1440x1000 | 51 | 51 | 0 | 0 | 0 | 0 |
| mobile 390x844 | 51 | 51 | 0 | 0 | 0 | 0 |

- mobileでは長い式の一部が `.math-block` 内スクロールになるが、ページ全体の横overflowはない。
- 旧数式画像要素は通常ページ上 **0**。
- 元画像 ↔ MathJax比較用ペア画像を `image001～image049` について生成して確認済み。

## 判定

**fem_7_2_2: 新厳密再監査 完了。**

旧レビューの「Pass 1 OK」判定は本ファイルの更新以前の履歴としては使用せず、2026-09-19の新厳密監査結果を正とする。
