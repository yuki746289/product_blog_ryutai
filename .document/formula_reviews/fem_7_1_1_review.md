# 数式レビュー: fem/fem_7_1_1.html

更新日: 2026-09-17

## 対象

- ページ: `fem/fem_7_1_1.html`
- タイトル: 質量収支式の離散化（3角形1次要素）
- 数式画像: `img/fem_d_mass_tri.files/`（`image001.png`～`image018.png`, `image020.png`, `image021.png`）
- 対象数: 20式
- 旧監査候補: `fem/fem_7_1_1_mathjax_audit.html`
- 再転記候補: `fem/fem_7_1_1_reaudit.html`
- 状態: **再転記候補のPass 1 = 20/20 OK / Pass 2待ち**

## 旧レビュー結果の位置づけ

旧レビューでは式変形の数学的整合性と、2D要素での積分領域 `V,dV` / `S,dS` の不一致候補等を確認した。
これらは理論上の検証結果であり、MathJax式が元画像と完全一致していることを保証しない。

## 2026-09-16以降の基準

- 元画像に `V,dV` とあれば、原文式ではそのまま `V,dV` を表示する。
- 理論上 `S,dS` が自然でも、原文式を直接置換しない。
- `x/y` と `X/Y`、`τ+Δτ` と `Δτ+τ` のように数学的意味が同じでも、元画像と表記が違えば不一致とする。
- 元画像に途中計算が展開されている場合、一般式や係数行列へ要約しない。
- 誤記候補は原文式の下へ補足する。
- 行列・係数・時刻添字・`Ma` 等を元画像と1文字・1成分単位で照合する。

## 通常ページ安全確認

通常ページを確認したところ、`image002.png` 相当の式だけがMathJaxのまま残っていた。
監査台帳の「元画像へ復元済」という記録と実態が一致していなかったため、通常ページの表示時には `js/site_formula_guard_v1_6.js` で `image002.png` を表示する安全ガードを追加した。

- レガシーHTML本体のCP932/Shift_JIS系エンコーディングは変更しない。
- 監査候補にはガードを適用しない。
- Pass 2完了までは通常ページに未確認MathJax式を表示しない。

## 元画像 ↔ 式対応付け

対象20式は `image001`～`image018`, `image020`, `image021`。`image019` は対象外。
元画像と式IDの1対1対応は20/20確認済み。

## 旧監査候補のPass 1結果

`fem_7_1_1_mathjax_audit.html` を元画像と直接比較した結果、**20/20式すべてNG**だった。

主な差分:

- 元画像の `X/Y` を候補が `x/y` に変更。
- 元画像の `V,dV` を候補が `S,dS` に変更。
- 元画像の `[N]^T` を候補が `[N]` に簡略化。
- 元画像の `Δτ+τ` を候補が `τ+Δτ` に並べ替え。
- `image008～015` 等の途中計算を、候補側で一般式・係数行列へ要約。
- `image011` は元画像が5項を3×3行列で展開した長い式だが、候補は `N_i=L_i` と行列置換の短い一般式へ要約。
- `image017` の先頭 `=`、`image018` の `[0]` 等を別表現へ変更。

旧候補は公開対象にしない。

## 再転記候補

以下を作成した。

- `.document/formula_reviews/fem_7_1_1_retranscription.md`
- `.document/formula_reviews/fem_7_1_1_image011_retranscription.md`
- `fem/fem_7_1_1_reaudit.html`

`fem_7_1_1_reaudit.html` は、左に元画像、右に再転記MathJaxを配置する左右比較ページである。通常ナビゲーションからはリンクしない。

## 再転記候補 Pass 1（2026-09-17）

20式を元画像と再度突き合わせ、以下を確認した。

- `x/y` と `X/Y` の大小文字。
- 積分領域 `V/S` と微小量 `dV/dS`。
- `[N]^T`, `[L]` 等の転置表記。
- 時刻添字を元画像どおり `Δτ+τ` の順で保持。
- 3×3行列の全成分。
- 途中計算を省略せず保持。
- `image017` の先頭 `=`、`image018` の `[0]` を保持。
- 原画像上の不自然表記も原文側では保持。

| No. | 元画像 | 再転記Pass 1 |
|---:|---|---|
| 1 | `image001` | OK |
| 2 | `image002` | OK |
| 3 | `image003` | OK |
| 4 | `image004` | OK |
| 5 | `image005` | OK |
| 6 | `image006` | OK |
| 7 | `image007` | OK |
| 8 | `image008` | OK |
| 9 | `image009` | OK |
| 10 | `image010` | OK |
| 11 | `image011` | OK |
| 12 | `image012` | OK |
| 13 | `image013` | OK |
| 14 | `image014` | OK |
| 15 | `image015` | OK |
| 16 | `image016` | OK |
| 17 | `image017` | OK |
| 18 | `image018` | OK |
| 19 | `image020` | OK |
| 20 | `image021` | OK |

**再転記候補 Pass 1結果: 20/20 OK。**

## 原文上の誤記・不自然表記

再転記候補では以下も元画像どおり保持した。

- `image002`: 列ベクトルが `N_1,N_1,N_3`。
- `image002`: 2D三角形要素で `V,dV` を使用。
- `image009`: 対流項末尾が `{V_x}`, `{V_y}`。
- `image012`: `{V_x}^{Δτ+τ}` 後に単独の `3`。
- `image013`: `i=j` 側途中式に `V`。

これらは原文式を変更せず、通常ページへMathJax化する際に必要であれば直下へ「補足」として理論上の解釈・修正版を分離表示する。

## 再監査チェック

- [x] 元画像の出現順を確認。
- [x] 欠番を確認。
- [x] 全式と元画像を1対1対応。
- [x] 旧候補20式をPass 1監査。
- [x] 元画像忠実版20式を再転記。
- [x] 再転記候補のPass 1 = 20/20。
- [ ] Pass 2 = 元画像とブラウザ上のMathJax描画を20/20確認。
- [ ] PC/スマホ表示確認済み。
- [ ] Pass 2完了後に通常ページへ反映。

## 現在の判定

**再転記候補: Pass 1合格20/20、Pass 2 = 0/20。通常ページは元画像表示を維持。**
