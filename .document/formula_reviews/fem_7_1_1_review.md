# 数式レビュー: fem/fem_7_1_1.html

更新日: 2026-09-19

## 対象

- ページ: `fem/fem_7_1_1.html`
- タイトル: 質量収支式の離散化（3角形1次要素）
- 正本: `img/fem_d_mass_tri.files/image001.png～image018.png, image020.png, image021.png`
- 対象数: **20式**
- `image019`: 対象外
- MathJaxブロック数: **20**
- 新厳密 Pass 1: **20/20**
- 新厳密 Pass 2: **20/20**
- HOLD: **0**
- 通常ページ反映: **完了**
- Browser QA: **PASS**

## 2026-09-19 新厳密監査方式

1. 元HP画像を最終正本とする。
2. 旧再転記記録は候補として利用し、最終判定は元画像で行う。
3. 元画像 ↔ MathJax完成描画を20式すべてペア画像化して直接比較する。
4. 数学的同値化はせず、式範囲・項順・符号・添字・大文字小文字・改行・句読点を正本に合わせる。
5. `x/y` と `X/Y`、`Δτ+τ` と `τ+Δτ`、`V/S`、`dV/dS` を重点確認する。
6. 原画像上の不自然表記も推測修正しない。

## 重点確認結果

- `image001`: 圧力対流項は小文字 `x/y`、速度発散項は大文字 `X/Y`。
- `image002`: 原画像どおり積分領域は **`V,dV`**、列ベクトル第2成分も **`N_1`**。
- `image003`: `[N]^T` を保持。
- `image006～016`: 時刻添字は原画像どおり **`Δτ+τ`**。
- `image009`: 対流項末尾は原画像どおり **`{V_x}`, `{V_y}`**。
- `image012`: `{V_x}^{Δτ+τ}` 後の単独 **`3`** を原画像どおり保持。
- `image013`: `i=j` 側途中式の **`V`** を原画像どおり保持。
- `image017`: 先頭 **`=`** を保持。
- `image018`: 右辺は原画像どおり **`[0]`**。

## 今回修正した主な差異

- `image002`: 連続等式を元画像どおり3行へ復元。
- `image003`: 元画像にない行末カンマ・ピリオドを除去。
- `image004`: 左辺と残差代入式を元画像どおり別行化。
- `image013`: `(i\ne j)` 周辺の不要カンマを除去。
- `image016`: 蓄積項、対流項、`1/Ma^2` 項を元画像どおり3行化。
- `image018`: 原画像どおり2行化。
- `image020`: 蓄積、対流、`1/Ma^2`＋右辺を3行化。
- `image021`: 係数項と `1/Ma^2`＋右辺を2行化。

## Browser / MathJax Pass 2

`Strict Formula Page QA` で最終確認。

| Viewport | Math blocks | Rendered | MathJax error | Unrendered | Uncontained overflow | Page overflow |
|---|---:|---:|---:|---:|---:|---|
| desktop 1440x1000 | 20 | 20 | 0 | 0 | 0 | 0 |
| mobile 390x844 | 20 | 20 | 0 | 0 | 0 | 0 |

- mobileでは長い式11ブロックが `.math-block` 内スクロールになる。
- ページ全体の横overflowはない。
- 旧数式画像要素は通常ページ上 **0**。
- 元画像 ↔ MathJax比較ペアを対象20式すべて生成し、直接確認済み。

## 判定

**fem_7_1_1: 新厳密再監査 完了。**

旧レビューより、2026-09-19の本結果を正とする。
