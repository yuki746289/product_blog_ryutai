# 数式レビュー: fem/fem_7_1_2.html

更新日: 2026-09-19

## 対象

- ページ: `fem/fem_7_1_2.html`
- タイトル: 運動量収支式の離散化（3角形1次要素）
- 正本: `img/fem_d_momentum_tri.files/image001.png` ～ `image042.png`
- 元画像数: **42**
- MathJaxブロック数: **45**（`image028`, `image032`, `image033` の再掲を含む）
- 新厳密 Pass 1: **42/42**
- 新厳密 Pass 2: **42/42**
- HOLD: **0**
- 通常ページ反映: **完了**
- Browser QA: **PASS**

## 2026-09-19 新厳密監査方式

1. 元HP画像を最終正本とする。
2. 旧Pass 1記録・既存LaTeXは候補として利用し、最終判定は元画像で行う。
3. 元画像 ↔ MathJax完成描画を42式すべてペア画像化して直接比較する。
4. 数学的同値化はしない。式範囲・項順・符号・添字・大文字小文字・改行・句読点を正本に合わせる。
5. `t/τ`, `i/j`, `x/y`, `X/Y`, `V/S/L`, `dV/dS/dL` を重点確認する。
6. 元画像の誤記候補は原文式では修正せず、必要なら式外で補足する。

## 重点確認結果

- `image003`: 2D式だが原画像どおり **`V,dV`** を保持。
- `image004`: `V_x`, `V_y` の末尾 **`=0`** を保持。圧力節点は **`P_{x,1}～P_{x,3}`**。
- `image005`: 左辺は `V,dV`、右辺は `S,dS`。
- `image013`: 表面張力項の `n_i` と積分の間にある原画像上の追加 **`L`** を保持。
- `image018`: y方向圧力項の先頭に原画像上 **`δ_{yi}` が無い**ため補完しない。
- `image027`: 線積分公式の分母は原画像どおり **`(p+q)!`**。面積積分途中の **`V`** も保持。
- `image032`: 表面張力係数は **`2K^*/We`**。
- `image038`: 2D式だが添字範囲は原画像どおり **`i=1,2,3`**。
- `image041/042`: 圧力項 `-[H_x]{P}`, `-[H_y]{P}` は原画像どおり `1/Re` の波括弧内。

## 今回修正した主な差異

- `image004`: 元画像にない行末カンマを除去。
- `image005`: 左辺積分と右辺残差積分を元画像どおり別行化。
- `image006/007`: 重力項を元画像どおり独立行化。
- `image008`: 重力項を独立行化。
- `image009`: 蓄積・対流・応力・境界・重力の5行構成へ修正。
- `image010～014`: 蓄積、対流、x項、y項、表面張力、重力を元画像の行構成へ復元。
- `image014`: `(i=1,2)` 前の不要カンマを除去。
- `image015`: 元画像注記 **「蓄積量」** をHTMLへ復元。
- `image017/018`: 元画像注記の「粘性項のx/y成分」「圧力項のx/y成分」をHTML側へ反映。
- `image020/026/033`: `(i=1,2)` 前の不要カンマを除去。
- `image027`: `(i\ne j)`, `(i=j)` 周辺の不要カンマを除去。
- `image037～040`: 元画像に合わせて改行を再構成し、添字範囲前の不要カンマを除去。
- `image038`: 圧力項と表面張力項を別行化。
- `image039/040`: 粘性主項・交差項・圧力項を元画像どおり個別行化。

## Browser / MathJax Pass 2

再利用可能な `Strict Formula Page QA` ワークフローで最終確認。

| Viewport | Math blocks | Rendered | MathJax error | Unrendered | Uncontained overflow | Page overflow |
|---|---:|---:|---:|---:|---:|---|
| desktop 1440x1000 | 45 | 45 | 0 | 0 | 0 | 0 |
| mobile 390x844 | 45 | 45 | 0 | 0 | 0 | 0 |

- mobileでは長い式の一部が `.math-block` 内スクロールになるが、ページ全体の横overflowはない。
- 旧数式画像要素は通常ページ上 **0**。
- 元画像 ↔ MathJax比較ペア画像を `image001～image042` について生成し、全42式を直接確認済み。

## 判定

**fem_7_1_2: 新厳密再監査 完了。**

旧レビューのPass 1判定より、2026-09-19の本結果を正とする。
