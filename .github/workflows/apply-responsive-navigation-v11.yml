name: Apply Responsive Navigation v1.1

on:
  push:
    branches:
      - develop
    paths:
      - '.github/workflows/apply-responsive-navigation-v11.yml'
      - '.github/scripts/apply-responsive-navigation-v11.py'

permissions:
  contents: write

jobs:
  implement:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout develop
        uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0

      - name: Apply implementation repair
        run: python3 .github/scripts/apply-responsive-navigation-v11.py

      - name: Show implementation diff
        run: |
          git diff -- menu.html
          git status --short

      - name: Commit implementation repair
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add menu.html
          if git diff --cached --quiet; then
            echo "No implementation repair changes to commit."
          else
            git commit -m "fix: remove duplicated legacy Init fragment"
            git push origin HEAD:develop
          fi
